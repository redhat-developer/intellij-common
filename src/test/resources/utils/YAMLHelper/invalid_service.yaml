apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: "test"
  namespace: "test"
invalid-TAG
  template:
    spec:
      containers:
        - image: "image"
          name: "first"
          env:
            - name: TARGET
              value: "Sample"
